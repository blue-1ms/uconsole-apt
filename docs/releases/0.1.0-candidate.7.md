# uConsole APT 0.1.0-candidate.7

Signed offline APT repository for `0.1.0-candidate.7`.

This is a prerelease bound to a test image that still requires the uConsole hardware checklist.
It is not a stable promotion. Verify `SHA256SUMS.asc` and the repository `InRelease` before
installation.
