# Security policy

Do not open a public issue containing credentials, private keys, authentication state or user
data. Report a suspected signing, package-integrity or update-path vulnerability through GitHub's
private vulnerability reporting for this repository.

Release assets never contain passwords, SSH keys, Wi-Fi or Tailscale state, npm/GitHub tokens or
pre-created user profiles. GitHub credentials are not required on uConsole clients because this
repository is public.
